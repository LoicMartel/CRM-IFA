import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "La Closing Académie - Booking",
};

export default function BookingNaznineLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
